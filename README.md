# 🌤️ Telegram Weather Bot

Телеграм-бот для получения актуального прогноза погоды по геолокации пользователя. Бот использует API Яндекс.Погоды для предоставления точной информации о текущих погодных условиях.

## ✨ Возможности

- 📍 Получение прогноза погоды по геолокации
- 🔄 Интерактивная кнопка для быстрого обновления прогноза
- 🌡️ Отображение температуры и ощущаемой температуры
- 💧 Информация о влажности воздуха
- 💨 Данные о скорости ветра
- 🔽 Атмосферное давление
- 🌈 Описание погодных условий с эмодзи
- ⚡ Быстрая обработка запросов (до 5 секунд)

## 📋 Требования

- Python 3.11 или выше
- Telegram Bot Token (от BotFather)
- API ключ Яндекс.Погоды

## 🔑 Получение API ключей

### Telegram Bot Token

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя вашего бота (например, "My Weather Bot")
   - Введите username бота (должен заканчиваться на `bot`, например, `my_weather_bot`)
4. BotFather отправит вам токен в формате: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
5. Сохраните этот токен - он понадобится для настройки

### Яндекс.Погода API Key

1. Перейдите на [Яндекс.Погода для разработчиков](https://yandex.ru/dev/weather/)
2. Нажмите "Подключить API"
3. Войдите в свой аккаунт Яндекс (или создайте новый)
4. Выберите тариф "Тестовый" (бесплатный, до 50 запросов в день)
5. Заполните форму заявки:
   - Укажите название проекта
   - Опишите цель использования API
6. После одобрения заявки (обычно в течение нескольких минут) вы получите API ключ
7. Скопируйте ключ из личного кабинета

## ⚙️ Настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd telegram-weather-bot
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```bash
cp .env.example .env
```

Откройте файл `.env` и заполните его своими API ключами:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
YANDEX_WEATHER_API_KEY=your_yandex_weather_api_key_here
```

**Важно:** Файл `.env` содержит конфиденциальные данные и не должен попадать в систему контроля версий. Он уже добавлен в `.gitignore`.

### 3. Настройка контроля доступа (опционально)

По умолчанию бот доступен всем пользователям Telegram. Если вы хотите ограничить доступ только определенным пользователям, добавьте в файл `.env` переменную `ALLOWED_USER_IDS`:

```env
ALLOWED_USER_IDS=123456789,987654321
```

#### Как получить User ID в Telegram

Существует несколько способов узнать свой User ID:

**Способ 1: Через специального бота**
1. Найдите в Telegram бота [@userinfobot](https://t.me/userinfobot) или [@getmyid_bot](https://t.me/getmyid_bot)
2. Отправьте боту любое сообщение
3. Бот ответит вам с информацией, включая ваш User ID

**Способ 2: Через логи бота (для разработчиков)**
1. Запустите бота без ограничений доступа
2. Отправьте боту команду `/start`
3. В логах бота вы увидите сообщение вида: `Команда /start от пользователя 123456789`
4. Число после "пользователя" - это ваш User ID

#### Примеры настройки

**Разрешить доступ только одному пользователю:**
```env
ALLOWED_USER_IDS=123456789
```

**Разрешить доступ нескольким пользователям:**
```env
ALLOWED_USER_IDS=123456789,987654321,555666777
```

**Разрешить доступ всем (по умолчанию):**
```env
# Оставьте переменную пустой или не указывайте её вообще
ALLOWED_USER_IDS=
```

При попытке несанкционированного доступа пользователь получит сообщение: "🚫 У вас нет доступа к этому боту", а попытка будет зафиксирована в логах.

## 🚀 Запуск

### Локальный запуск

#### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

#### 2. Запуск бота

```bash
python bot.py
```

Вы должны увидеть сообщение:

```
INFO - Загрузка конфигурации...
INFO - Конфигурация успешно загружена
INFO - Бот запускается...
```

Для остановки бота нажмите `Ctrl+C`.

### Запуск через Docker

#### 1. Сборка образа

```bash
docker-compose build
```

#### 2. Запуск контейнера

```bash
docker-compose up -d
```

Бот будет запущен в фоновом режиме.

#### 3. Просмотр логов

```bash
docker-compose logs -f
```

#### 4. Остановка бота

```bash
docker-compose down
```

### Альтернативный запуск через Docker (без docker-compose)

```bash
# Сборка образа
docker build -t telegram-weather-bot .

# Запуск контейнера
docker run -d --name weather-bot --env-file .env telegram-weather-bot

# Просмотр логов
docker logs -f weather-bot

# Остановка контейнера
docker stop weather-bot
docker rm weather-bot
```

## 📱 Использование бота

### Начало работы

1. Найдите вашего бота в Telegram по username, который вы указали при создании
2. Нажмите "Start" или отправьте команду `/start`
3. Бот отправит приветственное сообщение с инструкцией

### Получение прогноза погоды

1. Нажмите на иконку скрепки (📎) в поле ввода сообщения
2. Выберите "Геопозиция" или "Location"
3. Отправьте свое текущее местоположение
4. Бот обработает запрос и отправит актуальный прогноз погоды

### Обновление прогноза

После получения прогноза погоды вы увидите интерактивную кнопку **"🔄 Получить новый прогноз"** под сообщением с погодой.

**Как использовать:**
1. Нажмите на кнопку "🔄 Получить новый прогноз"
2. Бот автоматически запросит актуальные данные для вашей последней локации
3. Сообщение обновится с новым прогнозом
4. Кнопка останется доступной для последующих обновлений

**Преимущества:**
- Не нужно повторно отправлять геолокацию
- Быстрое получение актуальных данных одним нажатием
- Удобно для отслеживания изменений погоды в течение дня

**Примечание:** Если вы еще не отправляли геолокацию, бот попросит вас сначала отправить свое местоположение.

### Доступные команды

- `/start` - Начать работу с ботом и получить инструкцию
- `/help` - Показать справку по использованию бота

### Пример ответа бота

```
☀️ Ясно

🌡️ Температура: +22°C
🤔 Ощущается как: +20°C
💧 Влажность: 45%
💨 Ветер: 3.5 м/с
🔽 Давление: 755 мм рт.ст.

[🔄 Получить новый прогноз] ← интерактивная кнопка
```

## 🏗️ Структура проекта

```
telegram-weather-bot/
├── bot.py              # Основной модуль, запуск бота
├── config.py           # Управление конфигурацией
├── handlers.py         # Обработчики команд и сообщений
├── weather_api.py      # Клиент для Яндекс.Погода API
├── formatter.py        # Форматирование сообщений о погоде
├── user_storage.py     # Хранилище локаций пользователей
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример файла с переменными окружения
├── .gitignore          # Исключения для Git
├── Dockerfile          # Конфигурация Docker образа
├── docker-compose.yml  # Конфигурация Docker Compose
└── README.md           # Документация проекта
```

## 🔧 Технологии

- **Python 3.11** - Язык программирования
- **aiogram 3.4.1** - Асинхронная библиотека для Telegram Bot API
- **aiohttp 3.9.1** - Асинхронный HTTP клиент
- **python-dotenv 1.0.0** - Управление переменными окружения
- **Docker** - Контейнеризация приложения

## 🐛 Устранение неполадок

### Бот не запускается

**Проблема:** Ошибка "отсутствуют обязательные переменные окружения"

**Решение:**
- Убедитесь, что файл `.env` создан и содержит оба ключа
- Проверьте правильность написания имен переменных
- Убедитесь, что в значениях нет лишних пробелов или кавычек

### Бот не отвечает на сообщения

**Проблема:** Бот запущен, но не реагирует на команды

**Решение:**
- Проверьте, что токен бота правильный
- Убедитесь, что бот не заблокирован в Telegram
- Проверьте логи на наличие ошибок: `docker-compose logs` или вывод в консоли

### Ошибка при получении погоды

**Проблема:** "Сервис погоды временно недоступен"

**Решение:**
- Проверьте правильность API ключа Яндекс.Погоды
- Убедитесь, что не превышен лимит запросов (50 в день для тестового тарифа)
- Проверьте интернет-соединение
- Попробуйте отправить геолокацию еще раз через несколько минут

### Ошибка "Не удалось определить погоду для этой локации"

**Проблема:** Яндекс.Погода не может найти данные для указанных координат

**Решение:**
- Попробуйте отправить геолокацию еще раз
- Убедитесь, что вы отправляете именно геолокацию, а не адрес текстом
- Попробуйте другое местоположение

## 📝 Логирование

Бот ведет подробное логирование всех операций:

- Входящие команды и сообщения от пользователей
- Запросы к Яндекс.Погода API
- Ошибки с детальной информацией
- Статус запуска и остановки бота

Логи выводятся в stdout и доступны через:
- Консоль при локальном запуске
- `docker-compose logs -f` при запуске в Docker

## 🔒 Безопасность

- API ключи хранятся в переменных окружения, не в коде
- Файл `.env` исключен из Git через `.gitignore`
- Используется HTTPS для всех API запросов
- Обработка и логирование ошибок без раскрытия конфиденциальных данных

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте раздел "Устранение неполадок" выше
2. Убедитесь, что все зависимости установлены правильно
3. Проверьте логи бота на наличие ошибок
4. Убедитесь, что API ключи действительны и не истек срок их действия

---

Сделано с ❤️ для изучения разработки Telegram ботов
