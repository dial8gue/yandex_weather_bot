# Design Document

## Overview

Телеграм-бот на Python с использованием асинхронной библиотеки `aiogram` (версия 3.x) для взаимодействия с Telegram API и `aiohttp` для работы с Яндекс.Погода API. Бот работает в режиме polling, обрабатывает команды и геолокацию, возвращая пользователю актуальный прогноз погоды.

## Architecture

```
┌─────────────┐         ┌──────────────────┐         ┌─────────────────┐
│   Telegram  │ ◄─────► │  Weather Bot     │ ◄─────► │  Yandex Weather │
│   User      │         │  Application     │         │  API            │
└─────────────┘         └──────────────────┘         └─────────────────┘
                               │
                               ▼
                        ┌──────────────┐
                        │  Config &    │
                        │  Logging     │
                        └──────────────┘
```

### Архитектурные решения:

1. **Polling vs Webhook**: Используем polling для простоты развертывания (не требуется публичный URL и SSL)
2. **Асинхронная обработка**: Используем asyncio для эффективной обработки множественных запросов
3. **Модульная структура**: Разделение на модули для конфигурации, обработчиков, API клиента и утилит
4. **Router-based handlers**: Используем роутеры aiogram для организации обработчиков
5. **Middleware для контроля доступа**: Используем middleware aiogram для проверки User ID перед обработкой любых запросов (централизованная проверка доступа)

## Components and Interfaces

### 1. Bot Handler (`bot.py`)

Основной модуль приложения, инициализирует бота и запускает polling.

**Интерфейс:**
```python
async def main() -> None:
    """Инициализация и запуск бота"""
```

### 2. Handlers (`handlers.py`)

Модуль с обработчиками команд и сообщений.

**Интерфейс:**
```python
async def start_command(message: Message) -> None:
    """Обработчик команды /start"""
    
async def help_command(message: Message) -> None:
    """Обработчик команды /help"""
    
async def location_handler(message: Message) -> None:
    """Обработчик геолокации"""

class AccessControlMiddleware(BaseMiddleware):
    """Middleware для проверки доступа пользователей"""
    
    async def __call__(self, handler, event, data):
        """
        Проверить User ID перед обработкой запроса
        
        Если пользователь не в списке разрешенных:
        - Отправить сообщение о запрете доступа
        - Залогировать попытку несанкционированного доступа
        - Не вызывать handler
        """
```

### 3. Weather API Client (`weather_api.py`)

Модуль для асинхронного взаимодействия с Яндекс.Погода API.

**Интерфейс:**
```python
class WeatherAPIClient:
    def __init__(self, api_key: str):
        """Инициализация клиента с API ключом"""
        
    async def get_weather(self, lat: float, lon: float) -> dict:
        """
        Получить текущую погоду по координатам
        
        Args:
            lat: Широта
            lon: Долгота
            
        Returns:
            dict: Данные о погоде
            
        Raises:
            WeatherAPIError: При ошибке запроса к API
        """
        
    async def close(self) -> None:
        """Закрыть aiohttp сессию"""
```

### 4. Configuration (`config.py`)

Модуль для управления конфигурацией и переменными окружения.

**Интерфейс:**
```python
class Config:
    TELEGRAM_BOT_TOKEN: str
    YANDEX_WEATHER_API_KEY: str
    ALLOWED_USER_IDS: list[int]  # Список разрешенных User ID
    
    @classmethod
    def load() -> None:
        """Загрузить конфигурацию из переменных окружения"""
        
    @classmethod
    def validate() -> bool:
        """Проверить наличие всех необходимых параметров"""
        
    @classmethod
    def is_user_allowed(cls, user_id: int) -> bool:
        """
        Проверить, разрешен ли доступ пользователю
        
        Args:
            user_id: Telegram User ID
            
        Returns:
            bool: True если доступ разрешен (список пуст или user_id в списке)
        """
```

### 5. Message Formatter (`formatter.py`)

Утилиты для форматирования сообщений.

**Интерфейс:**
```python
def format_weather_message(weather_data: dict) -> str:
    """
    Форматировать данные о погоде в читаемое сообщение
    
    Args:
        weather_data: Данные от Weather API
        
    Returns:
        str: Отформатированное сообщение
    """
```

## Data Models

### Weather Data Structure

Структура данных, получаемых от Яндекс.Погода API:

```python
{
    "fact": {
        "temp": int,              # Температура в °C
        "feels_like": int,        # Ощущается как в °C
        "condition": str,         # Описание погоды
        "wind_speed": float,      # Скорость ветра м/с
        "humidity": int,          # Влажность %
        "pressure_mm": int        # Давление мм рт.ст.
    }
}
```

### Telegram Message Structure

Используется стандартная структура `Message` из библиотеки `aiogram`:

```python
message.location.latitude: float
message.location.longitude: float
message.chat.id: int
```

## Error Handling

### Типы ошибок:

1. **Configuration Errors**
   - Отсутствие API ключей
   - Обработка: Логирование и завершение приложения при старте

2. **Weather API Errors**
   - Недоступность API
   - Неверные координаты
   - Превышение лимита запросов
   - Обработка: Логирование + отправка пользователю сообщения об ошибке

3. **Telegram API Errors**
   - Ошибки отправки сообщений
   - Обработка: Логирование и повторная попытка (до 3 раз)

4. **Network Errors**
   - Timeout
   - Connection errors
   - Обработка: Логирование + сообщение пользователю о временной недоступности

5. **Access Control Errors**
   - Несанкционированный доступ (User ID не в списке разрешенных)
   - Обработка: Логирование попытки доступа с User ID + отправка сообщения о запрете доступа

### Error Response Format

```python
ERROR_MESSAGES = {
    "weather_api_unavailable": "⚠️ Сервис погоды временно недоступен. Попробуйте позже.",
    "invalid_location": "⚠️ Не удалось определить погоду для этой локации.",
    "general_error": "⚠️ Произошла ошибка. Попробуйте еще раз.",
    "access_denied": "🚫 У вас нет доступа к этому боту."
}
```

## Testing Strategy

### Unit Tests

1. **Weather API Client Tests**
   - Тест успешного получения погоды
   - Тест обработки ошибок API
   - Тест валидации координат

2. **Formatter Tests**
   - Тест форматирования полных данных
   - Тест форматирования с отсутствующими полями

3. **Config Tests**
   - Тест загрузки конфигурации
   - Тест валидации отсутствующих параметров

### Integration Tests

1. **Bot Handler Tests**
   - Тест команды /start
   - Тест команды /help
   - Тест обработки геолокации (с mock Weather API)

### Manual Testing

1. Отправка геолокации и проверка ответа
2. Проверка команд /start и /help
3. Проверка обработки ошибок (неверные данные, недоступность API)

## Dependencies

```
aiogram==3.4.1
aiohttp==3.9.1
python-dotenv==1.0.0
```

## Deployment Considerations

### Docker Container

Приложение упаковывается в минимальный Docker контейнер на базе `python:3.11-slim`.

**Dockerfile структура:**
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
CMD ["python", "bot.py"]
```

**docker-compose.yml для удобного запуска:**
```yaml
version: '3.8'
services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
```

### Environment Variables

1. **Configuration**
   - Создать `.env` файл с ключами:
     - `TELEGRAM_BOT_TOKEN`
     - `YANDEX_WEATHER_API_KEY`
     - `ALLOWED_USER_IDS` (опционально, список User ID через запятую, например: "123456789,987654321")
   - Добавить `.env` в `.gitignore`

2. **Running the Bot**
   - Локально: `python bot.py`
   - Docker: `docker-compose up -d`

3. **Logging**
   - Логи выводятся в stdout (доступны через `docker logs`)
   - Уровень логирования: INFO для production, DEBUG для разработки
